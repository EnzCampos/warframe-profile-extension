{
  "name": "warframe-profile-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "ci": "npm test && npm run build",
    "compile": "npm run build",
    "package": "npm run build",
    "publish:chrome": "node scripts/publish-chrome-web-store.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
